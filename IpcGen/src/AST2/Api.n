using System.Text;

namespace AST2
{
	[ Record ]
	public class Api
	{
		public Definitions : list[Definition];

		public override ToString() : string
		{
			def text = StringBuilder();

			foreach (definition in Definitions)
				_ = text.AppendLine(definition.ToString());

			text.ToString();
		}
	}
}
