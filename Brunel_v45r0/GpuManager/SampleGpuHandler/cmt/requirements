package SampleGpuHandler
version v1r1

# structure
branches cmt src

# dependencies
use GaudiPolicy *
use GpuHandler  * GpuManager

# main constituent
library SampleGpuHandler *.cpp

# the server uses Gaudi components to load handlers
# declare the constituent as a component library
apply_pattern component_library library=SampleGpuHandler

macro_append SampleGpuHandler_use_linkopts " -lGaudiPluginService"
